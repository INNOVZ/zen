/**
 * Channel Configuration Types - Compatibility Shim
 * 
 * @deprecated Import from '@/app/api/types' or '@/app/api/types/subscription' instead
 * This file exists for backward compatibility with existing imports.
 */

export type {
  ChannelInfo,
  SupportedChannels,
  ChannelConfiguration,
  ChannelConfigurations,
  ChannelUsage,
} from "@/app/api/types/subscription/channels";
