/**
 * ChatbotInfo Type - Compatibility Shim
 * 
 * @deprecated Import from '@/types/api' or '@/types' instead
 * This file exists for backward compatibility with existing imports.
 * 
 * @example
 * ```typescript
 * // Old way (still works)
 * import type { ChatbotInfo } from '@/types/ChatbotInfo';
 * 
 * // New way (preferred)
 * import type { ChatbotInfo } from '@/types';
 * ```
 */

export type { ChatbotInfo } from "./api/chat";



